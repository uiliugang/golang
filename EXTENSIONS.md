# 功能扩展计划

基于现有 RSS Reader + OPML 项目架构，以下是可扩展的方向。

## 一、基础功能增强

### 1.1 并发批量抓取

- **现状**: 导入 OPML 后串行抓取每个源，源多时很慢
- **方案**: 用 `sync.WaitGroup` + `goroutine` 并发抓取，`-concurrency` 控制最大并发数
- **价值**: 订阅源较多时显著提升速度

### 1.2 输出格式多样化

- **现状**: 仅支持终端文本输出
- **方案**: 新增 `-format` 参数，支持 `text` / `json` / `csv` / `html` 格式
- **价值**: 便于与其他工具链集成（如 `jq`、管道处理）

### 1.3 缓存机制

- **现状**: 每次运行都重新拉取所有源
- **方案**: 本地 SQLite（用 `modernc.org/sqlite` 零依赖）或 JSON 文件缓存，支持 `-cache-ttl` 设置过期时间
- **价值**: 减少重复网络请求，离线可查看最近结果

### 1.4 文章过滤与搜索

- **现状**: 无法过滤文章
- **方案**: 支持 `-filter-keyword` 关键词过滤标题/内容，`-since` 按日期过滤，`-author` 按作者过滤
- **价值**: 精准获取感兴趣的信息

### 1.5 错误处理与重试

- **现状**: 网络失败直接退出
- **方案**: 指数退避重试机制，`-retry` 设置重试次数，`-retry-delay` 设置初始等待时间
- **价值**: 提高网络不稳定场景的健壮性

## 二、OPML 管理增强

### 2.1 OPML 订阅源管理（CRUD）

- **现状**: 仅在导出时拼装 feeds 参数
- **方案**: 新增子命令体系：
  - `rss_reader opml add -title "BBC" -url ... -category News`
  - `rss_reader opml list`
  - `rss_reader opml remove -id 3`
  - `rss_reader opml update -id 3 -title "New Title"`
- **价值**: 完整管理订阅源列表，无需手动拼参数字符串

### 2.2 订阅源去重

- **现状**: 可导入重复 URL
- **方案**: 导入/添加时自动检测重复 URL，提供 `--dedup` 去重 flag
- **价值**: 避免重复抓取

### 2.3 OPML 校验与修复

- **方案**: 导入时校验 OPML 格式，自动修复常见问题（编码、缺少必要属性），输出详细错误报告
- **价值**: 兼容不规范的 OPML 文件

### 2.4 多 OPML 文件合并

- **方案**: 支持一次导入多个 OPML 文件，合并去重后统一管理或导出
- **价值**: 方便合并多个来源的订阅列表

## 三、数据持久化

### 3.1 SQLite 数据库存储

- **方案**: 引入 SQLite 存储订阅源和文章，支持：
  - 持久化订阅源列表（替代 OPML 文件作为主存储）
  - 记录抓取历史，标记已读/未读
  - 支持 `-db` 参数指定数据库路径
- **价值**: 数据持久化、支持更复杂的查询

### 3.2 增量抓取

- **方案**: 记录每篇文章的 GUID，新文章增量入库，仅输出新增内容
- **价值**: 避免重复输出已读文章

## 四、通知与自动化

### 4.1 定时抓取与通知

- **方案**: 新增 `daemon` 模式或 `cron` 兼容模式：
  - `rss_reader daemon -interval 30m` 后台持续运行
  - 新文章通过系统通知 (`notification`) 推送
- **价值**: 作为 RSS 提醒服务使用

### 4.2 Webhook / 回调

- **方案**: 匹配关键词后触发 Webhook，支持 Slack / Discord / 自定义 URL
- **价值**: 集成到现有工作流

### 4.3 邮件订阅推送

- **方案**: 将新文章汇总通过 SMTP 发送邮件，支持模板
- **价值**: 替代传统邮件订阅

## 五、Web 界面

### 5.1 本地 Web 服务

- **方案**: 用 `net/http` 或轻量框架（`gin` / `echo`）搭建 Web 界面：
  - 订阅源管理（增删改查）
  - 文章浏览、搜索、过滤
  - 标记已读/收藏
  - OPML 导入导出
- **价值**: 图形化管理

### 5.2 RESTful API

- **方案**: 提供 JSON API 供第三方客户端调用
- **价值**: 解耦前端/后端，支持移动端等

### 5.3 自托管阅读器

- **方案**: 在 Web 版基础上实现类 FreshRSS / Miniflux 的完整阅读体验
- **价值**: 完整自部署 RSS 解决方案

## 六、多协议支持

### 6.1 Atom Feed 支持

- **现状**: 仅支持 RSS 2.0
- **方案**: 增加 Atom (`application/atom+xml`) 格式的解析
- **价值**: Atom 是另一个主流订阅标准

### 6.2 JSON Feed 支持

- **方案**: 支持 [JSON Feed](https://www.jsonfeed.org/) v1.1 格式
- **价值**: 覆盖更多订阅源

### 6.3 自动格式嗅探

- **方案**: 根据 HTTP Content-Type 或 XML 根元素自动识别 RSS / Atom / JSON Feed
- **价值**: 无需用户指定格式

## 七、国际化与本地化

### 7.1 多语言支持

- **方案**: 提取字符串到 i18n 资源文件，通过 `-lang` 参数切换语言
- **价值**: 扩大用户群体

### 7.2 中文乱码处理

- **方案**: 自动检测并转换非 UTF-8 编码（GBK、Shift-JIS 等）
- **价值**: 兼容大量国内订阅源

## 八、测试与质量

### 8.1 集成测试

- **现状**: 仅有 OPML 包的单测
- **方案**: 为 `main.go` 的 handler 函数添加测试，使用 `httptest.Server` mock RSS 服务

### 8.2 模糊测试

- **方案**: 对 OPML 导入和 RSS 解析添加 Fuzz 测试
- **价值**: 发现边界情况和安全漏洞

### 8.3 性能基准测试

- **方案**: 为关键路径添加 Benchmark 测试，持续跟踪性能
- **价值**: 防止回归

### 8.4 GitHub Actions CI

- **方案**: 配置 CI 自动运行测试、lint（`golangci-lint`）、构建
- **价值**: 保证代码质量

## 九、架构演进

### 9.1 插件系统

- **方案**: 定义 `Plugin` 接口，支持输出插件（JSON/CSV/HTML）、通知插件（Slack/邮件）
- **价值**: 极高的可扩展性

### 9.2 配置文件

- **现状**: 所有配置通过 CLI 参数
- **方案**: 支持 YAML/TOML/JSON 配置文件 `rss_reader.yaml`，支持多 profile
- **价值**: 复用配置，减少命令行长度

### 9.3 管道模式

- **方案**: 支持标准输入/输出管道，组合多个命令：
  ```bash
  rss_reader -import-opml feeds.opml | rss_reader -filter "golang" | rss_reader -format json
  ```
- **价值**: Unix 哲学，灵活组合

## 十、优先推荐（按价值/投入比排序）

| 优先级 | 功能 | 预估代码量 | 核心价值 |
|--------|------|-----------|---------|
| P0 | 并发批量抓取 | ~50 行 | 即用即爽 |
| P0 | 错误重试 | ~80 行 | 提升稳定性 |
| P0 | 配置文件和 CRUD 管理 | ~300 行 | 核心体验提升 |
| P1 | SQLite 持久化 + 增量抓取 | ~500 行 | 数据不丢失 |
| P1 | 多输出格式 (JSON/CSV) | ~150 行 | 便于集成 |
| P1 | CI + 集成测试 | ~200 行 | 质量保障 |
| P2 | Web 界面 | ~1000+ 行 | 完整阅读器 |
| P2 | Atom / JSON Feed | ~300 行 | 协议兼容 |
| P3 | 插件系统 | ~400 行 | 架构升级 |
| P3 | 通知推送 | ~200 行 | 主动获取 |
